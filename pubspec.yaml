name: queueflow_mobile
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter
    
  cupertino_icons: ^1.0.8
  http: ^1.2.2
  mobile_scanner: ^7.2.0
  google_sign_in: ^6.2.1
  image_picker: ^1.1.2
  shared_preferences: ^2.3.2
  camera: ^0.11.0+2
  google_mlkit_face_detection: ^0.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/img/
